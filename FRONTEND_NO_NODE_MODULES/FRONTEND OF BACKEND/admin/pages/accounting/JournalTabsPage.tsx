// Legacy alias — TransactionsPage is the canonical implementation.
// Any remaining imports of JournalTabsPage will transparently use TransactionsPage.
export { default } from './TransactionsPage';
